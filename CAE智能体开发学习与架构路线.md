# CAE 智能体开发学习与架构路线

## 1. 从工具调用转向真实用户交互

当前测试方式是：

```text
请调用某个工具，完成某个仿真
```

这种方式适合开发阶段验证工具是否可用，但不是最终产品的理想交互方式。真实用户通常会表达目标和工程条件，例如：

```text
我要完成一个弯曲梁静力分析，网格尺寸是 0.005 m，
在右端施加 1000 N 向下载荷，左端固定。
```

智能体应先把自然语言转换为结构化配置，而不是立即启动求解：

```json
{
  "case": "Bending_3D_Beam",
  "analysis_type": "static_structural",
  "element_size_m": 0.005,
  "load": {
    "type": "force",
    "value_n": 1000,
    "direction": "negative_y"
  },
  "constraint": "fixed_left_end"
}
```

然后进行参数检查，并向用户确认：

```text
已识别：左端固定，右端施加 1000 N 向下力，网格尺寸 0.005 m。
是否确认开始求解？
```

推荐的真实交互流程是：

```text
用户自然语言
→ 提取仿真需求
→ 检查缺失信息
→ 向用户确认
→ 生成结构化配置
→ 调用仿真工具
→ 监控任务
→ 分析结果
→ 生成报告
```

## 2. 如何避免依赖 Codex 形成黑盒

在让 Codex 修改代码时，不要只要求“帮我实现”，还应要求它解释：

- 修改了哪些文件；
- 每个文件负责什么；
- 数据如何流动；
- 为什么采用当前设计；
- 如何进行最小测试；
- 失败时会发生什么；
- 哪些是设计假设，哪些是已经验证的事实。

可以固定使用以下提示词：

```text
请先解释设计方案和数据流，再修改代码。
修改后逐文件说明职责，并给出最小测试命令。
不要隐藏关键实现。
```

建议建立以下文档目录：

```text
docs/
├─ architecture.md       系统架构和数据流
├─ decisions/            设计决策记录
└─ experiments/          实验、参数和结果记录
```

学习一个功能时，可以遵循：

```text
观察现有代码
→ 手动复现
→ 自己解释
→ 修改一个小点
→ 编写测试
→ 记录结论
```

目标不是只做到“能运行”，还要做到“能解释、能修改、能验证”。

## 3. CAE 智能体需要的软件工程知识

### 3.1 立即需要

- Python 模块、包和虚拟环境；
- Git 分支、提交和回滚；
- JSON、YAML 和配置管理；
- 日志和异常处理；
- 单元测试和集成测试；
- CLI 命令行工具；
- 文件路径和进程管理。

### 3.2 Agent 开发必须掌握

- HTTP、REST 和 gRPC；
- 同步、异步、线程和进程；
- 状态机和工作流；
- 任务队列和超时；
- 重试、幂等性和断点恢复；
- 结构化输出和数据校验；
- 数据库和实验记录；
- 安全、权限和敏感信息管理。

### 3.3 产品化阶段需要

- Docker；
- CI/CD；
- 可观测性；
- Web API；
- 前端交互；
- 用户、任务和结果管理；
- 成本、性能和并发控制。

对 CAE Agent 特别重要的工程能力包括：

```text
工程输入校验
求解任务状态管理
失败重试
结果可信度判断
仿真日志追踪
结果版本管理
```

## 4. 智能体开发工程师的基础知识

建议重点补充六类基础：

```text
Python
计算机系统
网络与 API
数据结构与数据库
软件工程
机器学习与大模型应用
```

具体可以按以下顺序学习：

1. Python：模块、类、异常、类型标注和异步；
2. 计算机系统：进程、线程、文件、环境变量和进程间通信；
3. 网络：HTTP、JSON、WebSocket 和 gRPC；
4. 软件工程：Git、测试、日志、配置和 CI；
5. Agent：工具调用、状态图、记忆、评估和重试；
6. CAE：网格、边界条件、求解器、收敛性和结果可信度。

不建议先花很长时间脱离项目学习所有知识。更有效的方式是围绕真实问题补充：

```text
遇到虚拟环境问题 → 学 Python 环境
遇到 MCP 超时 → 学异步和任务状态
遇到结果不可信 → 学网格收敛
遇到工具失败 → 学异常、日志和重试
遇到多算例 → 学配置和抽象
```

## 5. 推荐的项目发展阶段

```text
阶段 1：弯曲梁固定参数自动化
阶段 2：网格、载荷、材料参数化
阶段 3：统一 CaseSpec 输入格式
阶段 4：LangGraph 编排确定性工作流
阶段 5：加入自然语言需求解析
阶段 6：加入用户确认和参数校验
阶段 7：加入异步任务和状态查询
阶段 8：增加第二、第三个 CAE 算例
阶段 9：建立评估集和回归测试
阶段 10：产品化为 API 或 Web 应用
```

## 6. 推荐系统架构

建议采用分层设计：

```text
CAE 核心服务
    ↑
PyMechanical 适配器
    ↑
MCP 适配器 / LangChain 工具
    ↑
LangGraph 工作流
    ↑
自然语言交互
```

各层职责如下：

- CAE 核心服务：执行确定性的网格、求解和后处理；
- PyMechanical 适配器：启动和控制 Mechanical；
- MCP 适配器：把仿真能力包装成标准工具；
- LangGraph：管理状态、步骤、重试和分支；
- LangChain：负责模型调用、工具调用和输出解析；
- 自然语言交互层：理解用户需求、补齐参数并请求确认。

不要让 LangChain 或 MCP 直接承载核心仿真逻辑。核心求解服务应该独立存在，这样将来即使不用 MCP、换成 Web API 或批处理，也不需要重写 ANSYS 部分。

## 7. 下一步建议

当前弯曲梁项目最值得做的下一步是定义统一的结构化输入模型，让智能体先完成：

```text
提取参数
→ 校验参数
→ 请求用户确认
→ 调用现有仿真流水线
```

建议先定义 `CaseSpec` 数据结构，再将网格尺寸、载荷、材料、边界条件和应力阈值逐步接入。完成后再使用 LangGraph 编排工作流，而不是一开始就构建复杂的全自动 Agent。

当前实现状态：`CaseSpec` 已扩展并校验载荷、方向和约束字段；这些字段会传入 Mechanical 执行上下文并写入结果日志。网格尺寸已实际驱动网格生成，载荷和约束对象的自动修改仍需根据当前模型树中的实际对象名称/API 单独确认后实现。
